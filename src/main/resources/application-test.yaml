spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowdesk_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: WARN
    com.flowdesk: INFO
